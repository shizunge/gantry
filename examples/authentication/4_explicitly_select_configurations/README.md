# Explicitly set labels on services

To authenticate with the same registry using multiple credentials, assign a unique configuration ID to each and set `gantry.auth.config` explicitly on each service.
