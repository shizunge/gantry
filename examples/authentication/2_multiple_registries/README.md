# Multiple registries

Specify credentials for multiple registries in [`gantry_registry_configs_file`](gantry_registry_configs_file), then Pass it to the *Gantry* container as a [Docker secret](https://docs.docker.com/engine/swarm/secrets/).
