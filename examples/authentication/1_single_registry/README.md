# Single registry

You want *Gantry* to pull images from one private registry (e.g. ghcr.io or registry.example.com) using a single set of credentials, set the following environment variables.

* `GANTRY_REGISTRY_HOST`
* `GANTRY_REGISTRY_PASSWORD`
* `GANTRY_REGISTRY_USER`
